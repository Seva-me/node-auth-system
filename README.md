## 🔐 Node.js Authentication System

A modular, production-ready authentication system built using **Node.js**, **Express**, **MongoDB**, **JWT**, **Joi validation**, and **Nodemailer (SMTP)**.  
This project includes secure **user registration**, **login**, **email verification**, **protected routes**, **refresh tokens** for scalable backend development.
